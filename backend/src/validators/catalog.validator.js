const Joi = require("joi");
module.exports = { query: { query: Joi.object({ search: Joi.string().max(100), category_id: Joi.number().integer().positive(), provider_id: Joi.number().integer().positive(), type: Joi.string().valid("subscription", "one_time", "both"), pincode: Joi.string().pattern(/^\d{6,10}$/), min_price: Joi.number().min(0), max_price: Joi.number().min(Joi.ref("min_price")), page: Joi.number().integer().min(1).default(1), limit: Joi.number().integer().min(1).max(100).default(20) }) }, params: { params: Joi.object({ id: Joi.number().integer().positive().required() }) } };
