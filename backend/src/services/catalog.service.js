const { Op } = require("sequelize");
const { Service, ServicePlan, Provider, Category, ServiceArea } = require("../models");
const include = [{ model: Provider, as: "provider", attributes: ["id", "business_name", "business_description", "logo_url", "average_rating"], where: { is_active: true, kyc_status: "approved" } }, { model: Category, as: "category", attributes: ["id", "name", "slug"] }, { model: ServicePlan, as: "plans", where: { is_active: true }, required: false }];
const services = async (query) => { const page = Number(query.page || 1); const limit = Number(query.limit || 20); const where = { is_active: true }; if (query.category_id) where.category_id = query.category_id; if (query.provider_id) where.provider_id = query.provider_id; if (query.type) where.type = query.type; if (query.search) where[Op.or] = [{ name: { [Op.like]: `%${query.search}%` } }, { description: { [Op.like]: `%${query.search}%` } }]; if (query.min_price !== undefined || query.max_price !== undefined) where.base_price = { ...(query.min_price !== undefined ? { [Op.gte]: query.min_price } : {}), ...(query.max_price !== undefined ? { [Op.lte]: query.max_price } : {}) }; if (query.pincode) include[0].include = [{ model: ServiceArea, as: "serviceAreas", where: { pincode: query.pincode }, required: true }]; const result = await Service.findAndCountAll({ where, include, distinct: true, limit, offset: (page - 1) * limit, order: [["created_at", "DESC"]] }); return { items: result.rows, pagination: { page, limit, total: result.count, pages: Math.ceil(result.count / limit) } }; };
const service = async (id) => { const result = await Service.findOne({ where: { id, is_active: true }, include }); if (!result) throw Object.assign(new Error("Service not found"), { statusCode: 404, code: "SERVICE_NOT_FOUND" }); return result; };
const categories = () => Category.findAll({ where: { is_active: true }, order: [["name", "ASC"]] });
const providers = (query) => Provider.findAll({ where: { is_active: true, kyc_status: "approved" }, attributes: ["id", "business_name", "business_description", "logo_url", "average_rating"], include: [{ model: Service, as: "services", where: { is_active: true }, required: false, attributes: ["id", "name", "type", "base_price", "unit"] }], order: [["average_rating", "DESC"]] });
module.exports = { services, service, categories, providers };
