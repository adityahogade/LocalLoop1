const { Op } = require("sequelize");
const { SupportTicket, SupportMessage } = require("../models");
const notifications = require("./notification.service");
const AppError = require("../utils/AppError");
const include = [{ model: SupportMessage, as: "messages", order: [["created_at", "ASC"]] }];
const canAccess = async (userId, roleId, id) => { const where = Number(roleId) === 1 ? { id } : { id, user_id: userId }; const ticket = await SupportTicket.findOne({ where, include }); if (!ticket) throw new AppError("Support ticket not found", 404, "TICKET_NOT_FOUND"); return ticket; };
const create = async (userId, data) => { const ticket = await SupportTicket.create({ ticket_code: `TKT-${Date.now().toString(36)}-${Math.floor(Math.random() * 1000)}`, user_id: userId, subject: data.subject, category: data.category, priority: data.priority || "medium" }); await SupportMessage.create({ ticket_id: ticket.id, sender_id: userId, message: data.message }); return canAccess(userId, 1, ticket.id); };
const list = (userId, roleId) => SupportTicket.findAll({ where: Number(roleId) === 1 ? {} : { user_id: userId }, include, order: [["updated_at", "DESC"]] });
const get = (userId, roleId, id) => canAccess(userId, roleId, id);
const addMessage = async (userId, roleId, id, data) => { const ticket = await canAccess(userId, roleId, id); if (["closed", "resolved"].includes(ticket.status) && Number(roleId) !== 1) throw new AppError("This ticket is no longer accepting messages", 409, "TICKET_CLOSED"); const message = await SupportMessage.create({ ticket_id: ticket.id, sender_id: userId, message: data.message, attachment_url: data.attachment_url || null }); if (Number(roleId) === 1) { if (ticket.status === "open") await ticket.update({ status: "in_progress" }); await notifications.createOnce({ user_id: ticket.user_id, type: "support_response", title: "Support responded", body: "There is a new response on your support ticket.", reference_type: "support_ticket", reference_id: ticket.id }); } return message; };
const update = async (userId, roleId, id, data) => { if (Number(roleId) !== 1) throw new AppError("Only administrators can update ticket status", 403, "FORBIDDEN"); const ticket = await canAccess(userId, roleId, id); const transitions = { open: ["in_progress", "closed"], in_progress: ["resolved", "closed", "open"], resolved: ["closed", "open"], closed: ["open"] }; if (data.status && !transitions[ticket.status].includes(data.status)) throw new AppError("Invalid ticket status transition", 409, "INVALID_TICKET_TRANSITION"); await ticket.update(data); return ticket; };
module.exports = { create, list, get, addMessage, update };
